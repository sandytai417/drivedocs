<style>
/* DriveDocs — 極簡＋手機流暢 */
*{box-sizing:border-box}
html,body{
  margin:0;padding:0;
  font:16px/1.45 -apple-system,"PingFang TC","Noto Sans TC","Microsoft JhengHei",sans-serif;
  color:#111;background:#fff;
  overflow-x:hidden;
  -webkit-text-size-adjust:100%;
  -webkit-tap-highlight-color:transparent;
  touch-action:manipulation;
  overscroll-behavior-y:contain;
}
button,input,select,textarea{font:inherit}
button{touch-action:manipulation}
a{color:#0b57d0;text-decoration:none}
.hidden,[hidden]{display:none!important}

.boot{min-height:100vh;min-height:100dvh;display:grid;place-items:center;text-align:center;padding:24px}
.boot__brand{margin:0;font-size:22px;font-weight:700}
.boot__hint{margin:8px 0 0;color:#666}

.shell{min-height:100vh;min-height:100dvh;display:flex;flex-direction:column;padding-bottom:env(safe-area-inset-bottom)}

/* —— 頂欄 —— */
.topbar{
  display:grid;
  grid-template-columns:auto 1fr auto;
  gap:8px;
  align-items:center;
  padding:8px 10px;
  padding-top:calc(8px + env(safe-area-inset-top));
  border-bottom:1px solid #e5e5e5;
  background:#fafafa;
  position:sticky;top:0;z-index:30;
}
.topbar__brand{font-weight:700;font-size:16px;white-space:nowrap}
.topbar__meta{color:#666;font-size:12px;text-align:right;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:42vw}
.topbar__search{grid-column:1/-1}
.topbar__search input{
  width:100%;padding:10px 12px;border:1px solid #ccc;border-radius:10px;
  font-size:16px; /* iOS 避免自動放大 */
  background:#fff;
}
.topbar__nav-desktop{display:none}
.topbar__extras{display:none}
.topbar__menu-btn{
  appearance:none;border:1px solid #ccc;background:#fff;border-radius:10px;
  min-width:44px;min-height:44px;padding:0 10px;cursor:pointer;font-size:18px;
}

/* —— 底部導覽（手機主操作） —— */
.bottom-nav{
  position:fixed;left:0;right:0;bottom:0;z-index:40;
  display:grid;grid-template-columns:repeat(5,1fr);
  gap:0;border-top:1px solid #e5e5e5;background:#fff;
  padding-bottom:env(safe-area-inset-bottom);
}
.bottom-nav button{
  appearance:none;border:0;background:transparent;
  min-height:56px;padding:8px 2px 6px;cursor:pointer;
  font-size:11px;color:#555;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:2px;
}
.bottom-nav button.is-active{color:#111;font-weight:700}
.bottom-nav .bn-ico{font-size:18px;line-height:1}

/* —— 更多選單（抽屜） —— */
.mobile-drawer-backdrop{
  position:fixed;inset:0;background:rgba(0,0,0,.35);z-index:50;
}
.mobile-drawer{
  position:fixed;left:0;right:0;bottom:0;z-index:60;
  background:#fff;border-radius:16px 16px 0 0;
  padding:12px 12px calc(16px + env(safe-area-inset-bottom));
  max-height:70vh;overflow:auto;
  box-shadow:0 -8px 24px rgba(0,0,0,.12);
}
.mobile-drawer__title{font-weight:700;margin:4px 4px 10px;font-size:15px}
.mobile-drawer .menu__item{
  display:block;width:100%;text-align:left;
  appearance:none;border:1px solid #e5e5e5;background:#fff;
  border-radius:10px;padding:14px 12px;margin:0 0 8px;cursor:pointer;font-size:15px;
}
.mobile-drawer .menu__item.is-active{background:#111;color:#fff;border-color:#111}

.main{
  padding:12px 12px calc(80px + env(safe-area-inset-bottom));
  max-width:1100px;width:100%;margin:0 auto;
  transition:opacity .14s ease;
  -webkit-overflow-scrolling:touch;
}
.main.is-loading{opacity:.72;pointer-events:none}
.soft-load{animation:softPulse 1.1s ease-in-out infinite}
@keyframes softPulse{0%,100%{opacity:.55}50%{opacity:1}}
@media (prefers-reduced-motion:reduce){
  .main{transition:none}
  .soft-load{animation:none}
}
h1{font-size:20px;margin:0 0 6px}
h2{font-size:16px;margin:0}
p{margin:0 0 8px}
.muted,.upload-hint{color:#666;font-size:13px}
.empty{color:#888;padding:12px 0}

.btn{
  appearance:none;border:1px solid #ccc;background:#fff;border-radius:10px;
  padding:10px 14px;cursor:pointer;min-height:44px;
}
.btn--primary{background:#111;color:#fff;border-color:#111}
.btn--ghost{background:transparent}
.btn--danger{background:#fff;color:#b00020;border-color:#f0b0b8}
.btn--sm{padding:8px 10px;min-height:40px;font-size:14px}
.btn:disabled{opacity:.5;cursor:not-allowed}

.stack{display:grid;gap:12px}
.card{border:1px solid #e5e5e5;border-radius:12px;background:#fff;margin-bottom:12px;overflow:hidden}
.card__hd{display:flex;justify-content:space-between;gap:8px;align-items:center;padding:12px;border-bottom:1px solid #f0f0f0}
.card__bd{padding:12px}
.card__title{margin:0;font-size:15px}
.card__sub{margin:2px 0 0;color:#666;font-size:12px}
.card__link{border:0;background:none;color:#0b57d0;cursor:pointer;padding:8px;min-height:44px}

.overview{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin:12px 0}
.overview .card{margin-bottom:0}
.stat{border:1px solid #e5e5e5;border-radius:12px;padding:12px}
.stat__value{font-size:22px;font-weight:700;margin:0}
.stat__label{color:#666;font-size:12px;margin:2px 0 0}
.stat__ico{display:none}

.field{display:flex;flex-direction:column;gap:4px;margin-bottom:10px}
.field label{font-size:13px;color:#444}
.field input,.field select,textarea{
  padding:10px 12px;border:1px solid #ccc;border-radius:10px;width:100%;
  font-size:16px; /* iOS 不放大 */
  min-height:44px;background:#fff;
}
.field-row{display:grid;grid-template-columns:1fr;gap:0}

.toolbar{display:flex;flex-direction:column;gap:8px;margin-bottom:10px}
.toolbar__right{display:flex;gap:8px;flex-wrap:wrap;align-items:center}
.pills{display:flex;flex-wrap:nowrap;gap:6px;overflow-x:auto;-webkit-overflow-scrolling:touch;padding-bottom:2px}
.pill{
  border:1px solid #ccc;background:#fff;border-radius:999px;
  padding:8px 12px;font-size:13px;cursor:pointer;white-space:nowrap;min-height:36px;flex:0 0 auto;
}
.pill.is-active{background:#111;color:#fff;border-color:#111}
.mini-search{padding:10px 12px;border:1px solid #ccc;border-radius:10px;width:100%;font-size:16px;min-height:44px}

.table-wrap{overflow:auto;-webkit-overflow-scrolling:touch;margin:0 -4px}
.table{width:100%;border-collapse:collapse;font-size:14px;min-width:520px}
.table th,.table td{border-bottom:1px solid #eee;padding:12px 8px;text-align:left;vertical-align:middle}
.table tbody tr{cursor:pointer}
.table tbody tr:active{background:#f5f5f5}
.badge{display:inline-block;padding:2px 8px;border-radius:999px;font-size:11px;border:1px solid #ddd}
.badge--renewal{background:#fff7e6}
.badge--new{background:#eef6ff}
.progress{display:flex;align-items:center;gap:6px}
.progress__bar{width:56px;height:6px;background:#eee;border-radius:99px;overflow:hidden}
.progress__fill{height:100%;background:#111;width:0}
.progress__pct{font-size:12px;color:#666}

@media(max-width:420px){
  .stat{padding:8px}
  .stat__value{font-size:18px}
}
@media(max-width:720px){
  .table{min-width:0;font-size:13px}
  .table th:nth-child(3),.table td:nth-child(3), /* 性別 */
  .table th:nth-child(4),.table td:nth-child(4), /* 生日 */
  .table th:nth-child(7),.table td:nth-child(7){display:none} /* 更新 */
}

.upload-grid{display:grid;gap:12px}
.dropzone{border:1px dashed #bbb;border-radius:12px;padding:20px 14px;text-align:center;background:#fcfcfc}
.dropzone.is-drag{background:#f0f6ff;border-color:#0b57d0}
.dropzone__title{font-weight:600;margin:0 0 4px}
.queue{list-style:none;margin:0;padding:0;display:grid;gap:8px}
.queue__item{border:1px solid #eee;border-radius:10px;padding:10px}
.upload-overall{border:1px solid #eee;border-radius:10px;padding:12px;background:#fafafa}
.upload-overall__row{display:flex;justify-content:space-between;align-items:center;margin-bottom:6px;gap:8px}
.upload-overall .progress__bar{width:100%;height:10px}
.upload-overall .progress__fill{transition:width .35s ease;min-width:2px}
.upload-overall.is-busy .progress__fill{
  background-image:repeating-linear-gradient(-45deg,#111 0 10px,#333 10px 20px);
  background-size:28px 28px;
  animation:upload-stripe .7s linear infinite
}
.upload-overall.is-success{border-color:#1a7f37;background:#f3faf4}
.upload-overall.is-success .progress__fill{background:#1a7f37;background-image:none;animation:none}
@keyframes upload-stripe{
  0%{background-position:0 0}
  100%{background-position:28px 0}
}

.activity{list-style:none;margin:0;padding:0}
.activity__item{display:grid;grid-template-columns:32px 1fr auto;gap:8px;padding:12px 0;border-bottom:1px solid #eee}
.activity__ico{width:32px;height:32px;border:1px solid #ddd;border-radius:50%;display:grid;place-items:center;font-size:12px}
.activity__title{margin:0;font-weight:600}
.activity__desc{margin:2px 0 0;color:#666;font-size:12px;word-break:break-word}
.activity__time{color:#888;font-size:12px}

.tabs{display:flex;flex-wrap:nowrap;gap:6px;margin:10px 0;overflow-x:auto;-webkit-overflow-scrolling:touch}
.tab{
  border:1px solid #ccc;background:#fff;border-radius:10px;
  padding:10px 14px;cursor:pointer;white-space:nowrap;min-height:44px;flex:0 0 auto;
}
.tab.is-active{background:#111;color:#fff;border-color:#111}
.detail-head{margin-bottom:8px}
.detail-head__meta{color:#666;font-size:13px;word-break:break-word}
.detail-head__right{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}
.folder-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-bottom:12px}
.folder-card{border:1px solid #e5e5e5;border-radius:12px;padding:12px;cursor:pointer;min-height:88px}
.folder-card.is-open{border-color:#111;background:#f7f7f7}
.folder-card__name{margin:0;font-weight:600}
.folder-card__meta{margin:4px 0 0;color:#666;font-size:12px}
.folder-card__flags{display:flex;flex-wrap:wrap;gap:6px;margin-top:8px}
.flag{border:1px solid #ddd;background:#fff;border-radius:999px;padding:6px 10px;font-size:12px;cursor:pointer;min-height:32px}
.flag.is-on,.flag.is-done{background:#111;color:#fff;border-color:#111}

.toasts{
  position:fixed;left:12px;right:12px;bottom:calc(72px + env(safe-area-inset-bottom));
  z-index:80;display:flex;flex-direction:column;gap:6px;
}
.toast{background:#111;color:#fff;padding:12px 14px;border-radius:10px;font-size:14px}
.toast--error{background:#b00020}

.modal-backdrop{
  position:fixed;inset:0;background:rgba(0,0,0,.4);
  display:flex;align-items:flex-end;justify-content:center;
  padding:0;z-index:70;
}
.modal{
  background:#fff;border-radius:16px 16px 0 0;padding:16px;
  width:100%;max-height:88vh;overflow:auto;
  padding-bottom:calc(16px + env(safe-area-inset-bottom));
}
.modal__actions{display:flex;justify-content:stretch;gap:8px;margin-top:12px}
.modal__actions .btn{flex:1}

.greeting{margin-bottom:12px}
.greeting__eyebrow,.greeting__actions,.polaroid-field,.polaroid,.feature-grid,.feature-card,.dot-logo,.privacy-chip,.navpill,.sidebar,.setup{display:none!important}

/* —— 平板／桌面 —— */
@media(min-width:860px){
  .topbar{
    display:flex;flex-wrap:wrap;gap:8px;align-items:center;
    padding:10px 16px;
  }
  .topbar__menu-btn,.bottom-nav,.mobile-drawer,.mobile-drawer-backdrop{display:none!important}
  .topbar__nav-desktop{display:flex;flex-wrap:wrap;gap:4px}
  .topbar__nav-desktop button{
    appearance:none;border:1px solid #ccc;background:#fff;border-radius:8px;
    padding:8px 12px;cursor:pointer;min-height:44px;
  }
  .topbar__nav-desktop button.is-active{background:#111;color:#fff;border-color:#111}
  .topbar__search{flex:1;min-width:220px;grid-column:auto}
  .topbar__extras{display:flex;gap:8px;align-items:center}
  .main{padding:16px 20px 40px}
  .field-row{grid-template-columns:1fr 1fr;gap:10px}
  .toolbar{flex-direction:row;justify-content:space-between;align-items:center}
  .mini-search{width:auto;min-width:200px}
  .upload-grid{grid-template-columns:1fr 1fr}
  .folder-grid{grid-template-columns:repeat(auto-fill,minmax(160px,1fr))}
  .modal-backdrop{align-items:center;padding:20px}
  .modal{border-radius:12px;width:min(560px,100%);padding-bottom:16px}
  .modal__actions{justify-content:flex-end}
  .modal__actions .btn{flex:none}
  .toasts{left:auto;right:16px;bottom:16px;width:min(340px,calc(100vw - 32px))}
  .table{min-width:0}
}
</style>
