DriveDocs 可複製原始檔（v260813y）
================================
客戶列表以掃描 Drive「客戶資料／注音／姓名」為準。
試算表只是索引，不掃到的夾不會出現在列表。

每個 *.txt 就是 Apps Script 要貼上的完整內容。
檔名去掉 .txt 就是 Apps Script 裡的檔名。
這次請覆蓋：
  Config.gs、App.html、Index.html
貼完請部署 → 新版本。畫面上應顯示 v260813y。

上傳讀檔變快：從雲端硬碟選取不必讀本機；本機照片會自動縮小再上傳。
